#! /usr/bin/env bash

#DIR=$(dirname $0);
DIR="$( cd "$( dirname "${BASH_SOURCE[0]}" )" >/dev/null 2>&1 && pwd )"
source "$DIR/os.bash"
source "$DIR/cecho.bash"
source "$DIR/check.bash"
source "$DIR/step.bash"
